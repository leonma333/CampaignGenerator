version: '3.7'

services:

  example:
    campaign_generator: campaign_generator
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - '4201:4200'
